Demo project Spring Cloud Stream with Kafka


Start the services with:
```bash
docker compose up -d
```
stop the services with:
```bash
docker compose down
```

kafka ui is available at http://localhost:8090

